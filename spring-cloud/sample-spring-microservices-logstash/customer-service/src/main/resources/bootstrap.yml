spring:
  application:
    name: customer-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: root
      username: user
      
server:  
  port: ${PORT:3333}
